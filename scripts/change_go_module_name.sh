#!/bin/bash

# OLD_MODULE="github.com/wen-git-acc/orderbook"
# NEW_MODULE="github.com/wen-git-acc/orderbook/v2"

# # Navigate to the server directory
# cd ../server || exit 1

# # Find and replace in .go files
# find . -type f -name '*.go' -exec sed -i "s|$OLD_MODULE|$NEW_MODULE|g" {} +

# echo "Module names updated from $OLD_MODULE to $NEW_MODULE."